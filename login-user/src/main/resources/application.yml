server:
  port: 8082

spring:
  application:
    name: login-user

  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
        namespace: public
        group: DEFAULT_GROUP

# 日志
logging:
  level:
    com.login: debug
